// PLANTILLA — copiar este archivo a app_secrets.dart y completar los valores.
// El archivo app_secrets.dart esta en .gitignore; este ejemplo si se sube a git.
class AppSecrets {
  const AppSecrets._();

  // Cloud name de tu cuenta Cloudinary (ej: 'mi-empresa')
  static const cloudinaryCloudName = 'ddjqpa8u9';

  // Nombre del upload preset unsigned (ej: 'control_cireles_upload')
  static const cloudinaryUploadPreset = 'cireles_upload';
}
